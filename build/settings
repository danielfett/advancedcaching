# This is a shell file
SOURCE="../advancedcaching"
RES="../res"
eval `grep "VERSION=" $SOURCE/core.py` # Set VERSION from core.py file
PKG="../dist/$DIST/advancedcaching-$VERSION"
PKGTMP=$PKG/temp/
PYSIDE_ASSISTANT=`pwd`/../PySideAssistant/psa
PYSIDE_ROOT=`pwd`/../PySideAssistant/

echo "Settings loaded:"
echo " Python source path is $SOURCE"
echo " Target path is $PKG"
echo " VERSION is $VERSION"
echo " Temporary path is $PKGTMP"
echo " Resources are in $RES"
